pipeline {
    
    agent any
    tools {
        maven 'maven'
    }
    
    stages {
        stage ('General') {
            steps {
                echo "Current branch name: ${env.BRANCH_NAME}"
            }
        }
        
	}
}

